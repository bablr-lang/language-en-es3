/* @macrome
 * @generatedby @bablr/macrome-generator-bablr
 * @generatedfrom ./statement.macro.js#feb4b6fe41d1c4c6ee24aab7f03741029baceb41
 * This file is autogenerated. Please do not edit it directly.
 * When editing run `npx macrome watch` then change the file this is generated from.
 */
import _applyDecs from "@babel/runtime/helpers/applyDecs2305";
import { re, spam as m } from '@bablr/boot';
import { eat, match, eatMatch, fail, o } from '@bablr/helpers/grammar';
import { AllowEmpty, CoveredBy, InjectFrom, Node } from '@bablr/helpers/decorators';
import * as productions from '@bablr/helpers/productions';
import { ReferenceTag } from '@bablr/agast-helpers/symbols';
export const noSemiStatements = ['IfStatement', 'ForStatement', 'SwitchStatement', 'BlockStatement'];
export const mixin = Base => {
  let _initProto, _ExpressionStatementDecs, _FunctionStatementDecs, _ReturnStatementDecs, _ThrowStatementDecs, _VariableDeclarationStatementDecs, _EmptyStatementDecs, _BlockStatementDecs, _IfStatementDecs, _WhileStatementDecs, _DoWhileStatementDecs, _SwitchStatementDecs, _ForStatementDecs, _LabeledStatementDecs, _BreakStatementDecs, _ContinueStatementDecs, _PunctuatorDecs;
  return class ES3StatementGrammar extends Base {
    static {
      [_initProto] = _applyDecs(this, [[AllowEmpty, 2, "StatementList"], [_ExpressionStatementDecs, 2, "ExpressionStatement"], [_FunctionStatementDecs, 2, "FunctionStatement"], [_ReturnStatementDecs, 2, "ReturnStatement"], [_ThrowStatementDecs, 2, "ThrowStatement"], [_VariableDeclarationStatementDecs, 2, "VariableDeclarationStatement"], [Node, 2, "VariableDeclarator"], [AllowEmpty, 2, "Statement"], [_EmptyStatementDecs, 2, "EmptyStatement"], [_BlockStatementDecs, 2, "BlockStatement"], [_IfStatementDecs, 2, "IfStatement"], [_WhileStatementDecs, 2, "WhileStatement"], [_DoWhileStatementDecs, 2, "DoWhileStatement"], [_SwitchStatementDecs, 2, "SwitchStatement"], [Node, 2, "SwitchCase"], [_ForStatementDecs, 2, "ForStatement"], [_LabeledStatementDecs, 2, "LabeledStatement"], [_BreakStatementDecs, 2, "BreakStatement"], [_ContinueStatementDecs, 2, "ContinueStatement"], [_PunctuatorDecs, 2, "Punctuator"]], [], 0, void 0, Base).e;
    }
    constructor(...args) {
      super(...args);
      _initProto(this);
    }
    *[(_ExpressionStatementDecs = [CoveredBy('Statement'), AllowEmpty, Node], _FunctionStatementDecs = [CoveredBy('Statement'), Node], _ReturnStatementDecs = [CoveredBy('Statement'), Node], _ThrowStatementDecs = [CoveredBy('Statement'), Node], _VariableDeclarationStatementDecs = [CoveredBy('Statement'), Node], _EmptyStatementDecs = [CoveredBy('Statement'), AllowEmpty, Node], _BlockStatementDecs = [CoveredBy('Statement'), Node], _IfStatementDecs = [CoveredBy('Statement'), Node], _WhileStatementDecs = [CoveredBy('Statement'), Node], _DoWhileStatementDecs = [CoveredBy('Statement'), Node], _SwitchStatementDecs = [CoveredBy('Statement'), Node], _ForStatementDecs = [CoveredBy('Statement'), Node], _LabeledStatementDecs = [CoveredBy('Statement'), Node], _BreakStatementDecs = [CoveredBy('Statement'), Node], _ContinueStatementDecs = [CoveredBy('Statement'), Node], _PunctuatorDecs = [Node, InjectFrom(productions)], "StatementList")]({
      ctx,
      s
    }) {
      let stmt, trivia, newline;
      while (yield match(re`/./s`)) {
        stmt = yield eat(m`<_Statement />`);
        if (s.resultPath?.previousSibling.tag.type === ReferenceTag && s.resultPath?.previousSibling.tag.value.type === '#') {
          trivia = s.resultPath.inner;
        } else {
          trivia = yield eatMatch(m`#: :Comment: <__Trivia /[ \n\r\t]|\/\/|\/\*/ />`);
        }
        newline = trivia && ctx.sourceTextFor(trivia).includes('\n');
        if (noSemiStatements.includes(stmt.type) && !(stmt.get('endToken') || newline)) break;
      }
    }
    *ExpressionStatement() {
      yield eat(m`expression+: <_Expression />`);
      yield eatMatch(m`endToken: <*Punctuator ';' />`, null, o({
        bind: true
      }));
    }
    *FunctionStatement() {
      yield eat(m`sigilToken: <*Keyword 'function' />`);
      yield eat(m`id: <Identifier />`);
      yield eat(m`openArgumentsToken: <*Punctuator '(' { balanced: ')' } />`);
      yield eat(m`params[]: <__List />`, o({
        element: m`<Identifier />`,
        allowTrailingSeparator: false,
        separator: m`separatorTokens[]: <*Punctuator ',' />`
      }));
      yield eat(m`closeArgumentsToken: <*Punctuator ')' { balancer: true } />`);
      yield eat(m`body: <BlockStatement />`);
    }
    *ReturnStatement() {
      yield eat(m`sigilToken: <*Keyword 'return' />`);
      yield eat(m`expression+$: <_Expression />`);
      yield eatMatch(m`endToken: <*Punctuator ';' />`, null, o({
        bind: true
      }));
    }
    *ThrowStatement() {
      yield eat(m`sigilToken: <*Keyword 'throw' />`);
      yield eat(m`expression+$: <_Expression />`);
      yield eatMatch(m`endToken: <*Punctuator ';' />`, null, o({
        bind: true
      }));
    }
    *VariableDeclarationStatement({
      props: {
        noSemi = false
      }
    }) {
      yield eat(m`sigilToken: <*Keyword 'var' />`);
      yield eat(m`declarations[]: <__List />`, o({
        element: m`<VariableDeclarator />`,
        allowTrailingSeparator: false,
        separator: m`separatorTokens[]: <*Punctuator ',' />`
      }));
      if (!noSemi) {
        yield eatMatch(m`endToken: <*Punctuator ';' />`, null, o({
          bind: true
        }));
      } else {
        yield eat(m`endToken: null`, null, o({
          bind: true
        }));
      }
    }
    *VariableDeclarator() {
      yield eat(m`target: <Identifier />`);
      if (yield match(re`/=/s`)) {
        yield eat(m`assignmentOperator: <*Punctuator '=' />`);
        yield eat(m`value+$: <_Expression />`, o({
          power: 32
        }));
      } else {
        yield eat(m`assignmentOperator: null`);
        yield eat(m`value+$: null`);
      }
    }
    *Statement({
      props: {
        allowEmpty = true
      }
    }) {
      yield eat(m`<__Any />`, [[m`<EmptyStatement /;/ />`, o({
        allowEmpty
      })], m`<BlockStatement '{' />`, m`<IfStatement 'if' />`, m`<WhileStatement 'while' />`, m`<DoWhileStatement 'do' />`, m`<SwitchStatement 'switch' />`, m`<ForStatement 'for' />`, m`<FunctionStatement /function [a-zA-Z$_]/ />`, m`<ReturnStatement 'return' />`, m`<ThrowStatement 'throw' />`, m`<VariableDeclarationStatement 'var' />`, m`<ExpressionStatement />`]);
    }
    *EmptyStatement({
      props: {
        allowEmpty = true
      }
    }) {
      if (allowEmpty) {
        yield eatMatch(m`endToken: <*Punctuator ';' />`, null, o({
          bind: true
        }));
      } else {
        yield eat(m`endToken: <*Punctuator ';' />`, null, o({
          bind: true
        }));
      }
    }
    *BlockStatement() {
      yield eat(m`openToken: <*Punctuator '{' { balanced: '}' } />`);
      yield eat(m`body[]: <__StatementList />`);
      yield eat(m`closeToken: <*Punctuator '}' { balancer: true } />`);
    }
    *IfStatement() {
      yield eat(m`sigilToken: <*Keyword 'if' />`);
      yield eat(m`openHeaderToken: <*Punctuator '(' { balanced: ')' } />`);
      yield eat(m`test+: <_Expression />`);
      yield eat(m`closeHeaderToken: <*Punctuator ')' { balancer: true } />`);
      yield eat(m`consequent: <_Statement />`, o({
        allowEmpty: false
      }));
      if (yield eatMatch(m`alternateSigilToken: <*Keyword 'else' />`)) {
        yield eat(m`consequent: <_Statement />`, o({
          allowEmpty: false
        }));
      } else {
        yield eat(m`alternate: null`);
      }
    }
    *WhileStatement() {
      yield eat(m`sigilToken: <*Keyword 'while' />`);
      yield eat(m`openHeaderToken: <*Punctuator '(' { balanced: ')' } />`);
      yield eat(m`test+$: <_Expression />`);
      yield eat(m`closeHeaderToken: <*Punctuator ')' { balancer: true } />`);
      yield eat(m`body: <_Statement />`);
    }
    *DoWhileStatement() {
      yield eat(m`sigilToken: <*Keyword 'do' />`);
      yield eat(m`body: <_Statement />`);
      yield eat(m`footerSigilToken: <*Keyword 'while' />`);
      yield eat(m`openFooterToken: <*Punctuator '(' { balanced: ')' } />`);
      yield eat(m`test+$: <_Expression />`);
      yield eat(m`closeFooterToken: <*Punctuator ')' { balancer: true } />`);
      yield eatMatch(m`endToken: <*Punctuator ';' />`, null, o({
        bind: true
      }));
    }
    *SwitchStatement() {
      yield eat(m`sigilToken: <*Keyword 'switch' />`);
      yield eat(m`openDiscriminantToken: <*Punctuator '(' { balanced: ')' } />`);
      yield eat(m`discriminant+$: <_Expression />`);
      yield eat(m`closeDiscriminantToken: <*Punctuator ')' { balancer: true } />`);
      yield eat(m`openCasesToken: <*Punctuator '{' { balanced: '}' } />`);
      yield eatMatch(m`cases[]: <SwitchCase /(case|default)\b/ />`);
      yield eat(m`closeCasesToken: <*Punctuator '}' { balancer: true } />`);
    }
    *SwitchCase({
      intrinsicValue,
      ctx
    }) {
      switch (ctx.sourceTextFor(intrinsicValue)) {
        case 'case':
          yield eat(m`sigilToken: <*Keyword 'case' />`);
          yield eat(m`condition+$: <_Expression />`);
          break;
        case 'default':
          yield eat(m`sigilToken: <*Keyword 'default' />`);
          yield eat(m`condition: null`);
          break;
        default:
          yield fail();
          break;
      }
      yield eat(m`bodySeparatorToken: <*Punctuator ':' />`);
      while (yield match(re`/./`)) yield eat(m`statements[]: <_Statement />`);
    }
    *ForStatement() {
      yield eat(m`sigilToken: <*Keyword 'for' />`);
      yield eat(m`openHeaderToken: <*Punctuator '(' { balanced: ')' } />`);
      if (!(yield eatMatch(m`init+$: <VariableDeclarationStatement />`, o({
        noSemi: true
      })))) {
        yield eatMatch(m`init+$: <_Expression />`, o({
          noIn: true
        }));
      }
      let in_ = yield eatMatch(m`inToken: <*Keyword 'in' />`, o({}), o({
        bind: true
      }));
      if (in_) {
        yield eatMatch(m`right+$: <_Expression />`);
      } else {
        yield eat(m`testSeparatorToken: <*Punctuator ';' />`);
        yield eatMatch(m`test+$: <_Expression />`);
        yield eat(m`updateSeparatorToken: <*Punctuator ';' />`);
        yield eatMatch(m`update+$: <_Expression />`);
        yield eat(m`closeHeaderToken: <*Punctuator ')' { balancer: true } />`);
        yield eat(m`body: <_Statement />`);
      }
    }
    *LabeledStatement() {
      yield eat(m`label: <Identifier />`);
      yield eat(m`sigilToken: <*Puncuator ':' />`);
      yield eat(m`body: <*Statement />`);
    }
    *BreakStatement() {
      yield eat(m`sigilToken: <*Keyword 'break' />`);
      yield eatMatch(m`label: <Identifier />`);
    }
    *ContinueStatement() {
      yield eat(m`sigilToken: <*Keyword 'continue' />`);
      yield eatMatch(m`label: <Identifier />`);
    }
    *Punctuator() {}
  };
};
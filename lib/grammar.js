/* @macrome
 * @generatedby @bablr/macrome-generator-bablr
 * @generatedfrom ./grammar.macro.js#d0a8e01190d1217627b628f1c99aab943f5647c3
 * This file is autogenerated. Please do not edit it directly.
 * When editing run `npx macrome watch` then change the file this is generated from.
 */
import _applyDecs from "@babel/runtime/helpers/applyDecs2305";
var _functionMixin;
let _initProto, _ParenthesisExpressionDecs, _IdentifierDecs, _ListDecs, _LiteralDecs, _PunctuatorDecs, _AnyDecs, _AllDecs, _OptionalDecs;
import { spam as m, re } from '@bablr/boot';
import { Node, InjectFrom, AllowEmpty, CoveredBy } from '@bablr/helpers/decorators';
import { eat, eatMatch, o, shiftMatch, fail, match } from '@bablr/helpers/grammar';
import { triviaEnhancer } from '@bablr/helpers/trivia';
import * as productions from '@bablr/helpers/productions';
import * as Comment from '@bablr/language-en-c-comments';
import { mixin as functionMixin } from './mixins/function.js';
import { mixin as jsonMixin } from './mixins/json.js';
import { mixin as statementMixin } from './mixins/statement.js';
import { mixin as logicMixin, unaryPrefixOperatorAlternatives } from './mixins/logic.js';
import * as Regex from './regex.js';
import { buildPattern } from '@bablr/helpers/builders';
export { powerLevels, assignmentOperators, assignmentOperatorAlternatives, unaryPrefixOperators, unaryPrefixOperatorAlternatives, unaryPostfixOperators, unaryPostfixOperatorAlternatives, getBinaryOperatorAlternatives } from './mixins/logic.js';
export const dependencies = {
  Regex,
  Comment
};
export const canonicalURL = 'https://bablr.org/languages/universe/es3';
export const defaultMatcher = m`.+: <_Expression />`;
export const reservedWords = Object.freeze(['abstract', 'boolean', 'break', 'byte', 'case', 'catch', 'char', 'continue', 'debugger', 'default', 'delete', 'do', 'double', 'else', 'eval', 'false', 'final', 'finally', 'float', 'for', 'function', 'goto', 'if', 'implements', 'in', 'instanceof', 'int', 'interface', 'long', 'native', 'new', 'null', 'package', 'private', 'protected', 'public', 'return', 'short', 'static', 'switch', 'synchronized', 'this', 'throw', 'throws', 'transient', 'true', 'try', 'typeof', 'var', 'void', 'volatile', 'while', 'with', 'yield']);
let reservedWords_ = new Set(reservedWords);
export const atrivialGrammar = class ES3Grammar extends (_functionMixin = functionMixin(jsonMixin(statementMixin(logicMixin(Object))))) {
  static {
    [_initProto] = _applyDecs(this, [[[AllowEmpty, Node], 2, "Program"], [_ParenthesisExpressionDecs, 2, "ParenthesisExpression"], [_IdentifierDecs, 2, "Identifier"], [_ListDecs, 2, "List"], [Node, 2, "Keyword"], [_LiteralDecs, 2, "Literal"], [_PunctuatorDecs, 2, "Punctuator"], [_AnyDecs, 2, "Any"], [_AllDecs, 2, "All"], [_OptionalDecs, 2, "Optional"]], [], 0, void 0, _functionMixin).e;
  }
  constructor(...args) {
    super(...args);
    _initProto(this);
  }
  *[(_ParenthesisExpressionDecs = [CoveredBy('Expression'), Node], _IdentifierDecs = [CoveredBy('Expression'), Node], _ListDecs = [AllowEmpty, InjectFrom(productions)], _LiteralDecs = [Node, InjectFrom(productions)], _PunctuatorDecs = [Node, InjectFrom(productions)], _AnyDecs = InjectFrom(productions), _AllDecs = InjectFrom(productions), _OptionalDecs = [AllowEmpty, InjectFrom(productions)], Symbol.for('@bablr/fragment'))]({
    props: {
      rootMatcher
    }
  }) {
    // needed for the trivia plugin
    yield eat(rootMatcher);
  }
  *Program() {
    yield eat(m`body[]: <__StatementList />`);
  }
  *Expression({
    props: {
      power = 34,
      noIn = false
    },
    s
  }) {
    let res;
    if (!s.holding) {
      if (res = yield eatMatch(m`<ParenthesisExpression '(' />`)) {} else if (res = yield eatMatch(m`<__JSONExpression />`)) {} else if (res = yield eatMatch(m`:Regex: <Pattern '/' />`)) {} else if (res = yield eatMatch(m`<ThisExpression 'this' />`)) {} else if (res = yield eatMatch(m`<FunctionExpression 'function' />`)) {} else if (power >= 4 && (res = yield eatMatch(m`<UnaryExpression ${buildPattern(unaryPrefixOperatorAlternatives)} />`))) {} else if (power >= 4 && (res = yield eatMatch(m`<NewExpression 'new' />`, o({
        power
      })))) {} else if (res = yield eatMatch(m`<Identifier />`)) {}
    } else {
      res = yield eatMatch(m`<__LogicExpression />`, o({
        power,
        noIn
      }));
    }
    if (res) {
      return shiftMatch(m`<_Expression />`, o({
        power
      }));
    }
  }
  *ParenthesisExpression() {
    yield eat(m`openExpressionToken: <*Punctuator '(' { balanced: ')' }/>`);
    yield eat(m`expression+: <_Expression />`);
    yield eat(m`closeExpressionToken: <*Punctuator ')' { balancer: true } />`);
  }
  *Identifier({
    props: {
      scoped = true
    },
    ctx
  }) {
    let id = ctx.sourceTextFor(yield eat(m`value: <*Literal /[a-zA-Z_$][a-zA-Z\d_$]*/ />`));
    if (scoped && reservedWords_.has(id)) yield fail();
  }
  *List() {}
  *Keyword({
    ctx,
    literalValue,
    s
  }) {
    if (!literalValue) throw new Error('Intrinsic productions must have value');
    yield eat(ctx.sourceTextFor(literalValue.value));
    if (s.span !== 'Escape') {
      if (!(yield match(re`/[ \n\r\t]|\/\/|\/\*|$|[*&^%$#@!(){}[\].,/\\'";:=+|\u0061~-]/`))) {
        yield fail();
      }
    }
  }
  *Literal() {}
  *Punctuator() {}
  *Any() {}
  *All() {}
  *Optional() {}
};
export const grammar = triviaEnhancer({
  triviaIsAllowed: s => s.span === 'Bare',
  triviaMatcher: m`#: :Comment: <__Trivia /[ \n\r\t]|\/\/|\/\*/ />`
}, atrivialGrammar);